<!DOCTYPE html>
<html>
  <head>
    <title>Beauty Choice by MM</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">

    <header class="bg-white shadow">
      <div class="container mx-auto px-4 py-6 flex justify-between items-center">
        <h1 class="text-3xl font-bold text-gray-900"><%= link_to 'Beauty Choice by MM', root_path, class: "hover:text-blue-500" %></h1>
        <nav class="flex space-x-8 text-lg">
          <% Category.all.each do |category| %>
            <%= link_to category.name, category_path(category), class: "text-gray-700 hover:text-blue-500 transition duration-300" %>
          <% end %>
          <%= link_to 'Cart', cart_path, class: "text-gray-700 hover:text-blue-500 transition duration-300" %>
          <%= link_to 'About', about_path, class: "text-gray-700 hover:text-blue-500 transition duration-300" %>
          <%= link_to 'Contact', contact_path, class: "text-gray-700 hover:text-blue-500 transition duration-300" %>
        </nav>
      </div>
    </header>

    <main class="container mx-auto px-4 py-6">
      <%= yield %>
    </main>

    <footer class="bg-white shadow mt-6">
      <div class="container mx-auto px-4 py-6">
        <p class="text-center text-gray-600">&copy; 2024 Beauty Choice by MM</p>
      </div>
    </footer>

  </body>
</html>
