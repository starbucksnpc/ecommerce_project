<!DOCTYPE html>
<html>
  <head>
    <title>Ecommerce</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">

    <header class="bg-white shadow">
      <div class="container mx-auto px-4 py-6 flex justify-between items-center">
        <h1 class="text-3xl font-bold"><%= link_to 'Ecommerce', root_path %></h1>
        <nav class="flex space-x-4">
          <% Category.all.each do |category| %>
            <%= link_to category.name, category_path(category), class: "text-blue-500 hover:underline" %>
          <% end %>
          <%= link_to 'Carts', carts_path, class: "text-blue-500 hover:underline" %>
          <%= link_to 'About', about_path, class: "text-blue-500 hover:underline" %>
          <%= link_to 'Contact', contact_path, class: "text-blue-500 hover:underline" %>
        </nav>
      </div>
    </header>

    <main class="container mx-auto px-4 py-6">
      <%= yield %>
    </main>

    <footer class="bg-white shadow mt-6">
      <div class="container mx-auto px-4 py-6">
        <p class="text-center text-gray-600">&copy; 2024 My Ecommerce Site</p>
      </div>
    </footer>

  </body>
</html>
